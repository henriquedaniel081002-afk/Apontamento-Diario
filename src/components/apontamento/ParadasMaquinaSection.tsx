import React, { useId, useState } from 'react';
import { Check, Edit2, Plus, Settings, Trash2 } from 'lucide-react';
import { ParadaMaquinaItem } from '../../types';
import { ConfirmModal } from '../common/ConfirmModal';
import { Button, EmptyState, FieldError, Surface } from '../common/ui';

interface Props { itens: ParadaMaquinaItem[]; onAdd: (item: ParadaMaquinaItem) => void; onUpdate: (item: ParadaMaquinaItem) => void; onDelete: (id: string) => void; }

export const ParadasMaquinaSection: React.FC<Props> = ({ itens, onAdd, onUpdate, onDelete }) => {
  const maquinaId = useId(); const inicioId = useId(); const fimId = useId(); const obsId = useId();
  const [maquinaEquipamento, setMaquinaEquipamento] = useState(''); const [horaInicio, setHoraInicio] = useState(''); const [horaFim, setHoraFim] = useState(''); const [observacao, setObservacao] = useState('');
  const [editingId, setEditingId] = useState<string | null>(null); const [deleteId, setDeleteId] = useState<string | null>(null); const [error, setError] = useState<string | null>(null);
  const clear = () => { setMaquinaEquipamento(''); setHoraInicio(''); setHoraFim(''); setObservacao(''); setError(null); };
  const submit = (event: React.FormEvent) => { event.preventDefault(); const item: ParadaMaquinaItem = { id: editingId || `maquina-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`, maquinaEquipamento: maquinaEquipamento.trim(), horaInicio, horaFim, observacao: observacao.trim() }; editingId ? onUpdate(item) : onAdd(item); setEditingId(null); clear(); };
  const edit = (item: ParadaMaquinaItem) => { setEditingId(item.id); setMaquinaEquipamento(item.maquinaEquipamento); setHoraInicio(item.horaInicio); setHoraFim(item.horaFim); setObservacao(item.observacao); setError(null); };
  return <Surface as="section" className="record-industrial overflow-hidden" aria-labelledby="maquina-title"><div className="flex items-start gap-3 border-b border-[var(--border-subtle)] p-5 sm:p-6"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-rose-400/10 text-rose-300"><Settings className="h-5 w-5" /></span><div><p className="mb-1 text-xs font-semibold uppercase tracking-[0.14em] text-rose-300">Ocorrência opcional</p><h2 id="maquina-title" className="text-lg font-bold text-[var(--text-primary)]">Parada por Máquina Quebrada</h2><p className="mt-1 text-sm leading-6 text-[var(--text-secondary)]">Registre cada equipamento parado separadamente. Os campos podem ser preenchidos parcialmente e complementados depois.</p></div></div><div className="space-y-5 p-5 sm:p-6"><form onSubmit={submit} className="space-y-4 rounded-2xl border border-[var(--border-subtle)] bg-[var(--surface-muted)] p-4"><div className="grid gap-4 md:grid-cols-2"><label htmlFor={maquinaId}><span className="mb-2 block text-sm font-medium text-[var(--text-primary)]">Máquina / Equipamento</span><input id={maquinaId} value={maquinaEquipamento} onChange={(e) => { setMaquinaEquipamento(e.target.value); setError(null); }} className="field-control" placeholder="Ex.: Bobinadeira 03" /></label><label htmlFor={inicioId}><span className="mb-2 block text-sm font-medium text-[var(--text-primary)]">Hora de início</span><input id={inicioId} type="time" value={horaInicio} onChange={(e) => { setHoraInicio(e.target.value); setError(null); }} className="field-control [color-scheme:dark]" /></label><label htmlFor={fimId}><span className="mb-2 block text-sm font-medium text-[var(--text-primary)]">Hora de fim</span><input id={fimId} type="time" value={horaFim} onChange={(e) => { setHoraFim(e.target.value); setError(null); }} className="field-control [color-scheme:dark]" /></label><label htmlFor={obsId}><span className="mb-2 block text-sm font-medium text-[var(--text-primary)]">Observações</span><textarea id={obsId} rows={3} value={observacao} onChange={(e) => { setObservacao(e.target.value); setError(null); }} className="field-control resize-y" placeholder="Descreva o problema ocorrido" /></label></div><FieldError role="alert">{error}</FieldError><div className="flex justify-end gap-2 border-t border-[var(--border-subtle)] pt-4">{editingId && <Button variant="ghost" onClick={() => { setEditingId(null); clear(); }}>Cancelar edição</Button>}<Button type="submit" leftIcon={editingId ? <Check className="h-4 w-4" /> : <Plus className="h-4 w-4" />}>{editingId ? 'Salvar parada' : 'Adicionar parada'}</Button></div></form>{itens.length === 0 ? <EmptyState icon={<Settings className="h-6 w-6" />} title="Nenhuma máquina quebrada" description="Não é necessário adicionar nada quando não houver ocorrência." /> : <ul className="space-y-2">{itens.map((item, index) => <li key={item.id} className="rounded-2xl border border-[var(--border-subtle)] bg-[var(--surface-muted)] p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wider text-rose-300">Parada #{index + 1}</p><p className="mt-1 font-bold text-[var(--text-primary)]">{item.maquinaEquipamento || 'Máquina não informada'}</p><p className="mt-1 text-sm text-[var(--text-secondary)]">{item.observacao || 'Observação não informada'}</p><p className="mt-2 text-xs font-semibold text-[var(--text-tertiary)]">{item.horaInicio || 'Início não informado'} → {item.horaFim || 'Em andamento'}</p></div><div className="flex gap-1"><Button size="icon" variant="ghost" aria-label="Editar parada" onClick={() => edit(item)}><Edit2 className="h-4 w-4" /></Button><Button size="icon" variant="ghost" aria-label="Excluir parada" onClick={() => setDeleteId(item.id)}><Trash2 className="h-4 w-4" /></Button></div></div></li>)}</ul>}</div><ConfirmModal isOpen={Boolean(deleteId)} title="Excluir parada?" description="Esta ocorrência será removida do apontamento." confirmLabel="Excluir" onConfirm={() => { if (deleteId) onDelete(deleteId); if (editingId === deleteId) { setEditingId(null); clear(); } setDeleteId(null); }} onCancel={() => setDeleteId(null)} /></Surface>;
};
